<div>
  <h1>My Tasks</h1>
    <ul class="list-group">
      <% @tasks.each do |task| %>
        <li class="list-group-item"><%= link_to task.name, task_path(task)%></li>
      <% end %>
  </ul>
</div>

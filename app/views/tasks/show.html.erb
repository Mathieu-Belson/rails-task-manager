<div class>
<h1> <%= @task.name %> </h1>

  <p> <%= @task.details %> </p>

  <p> <%=link_to 'Home', tasks_path %> </p>

  <p> <%= link_to 'Back', edit_task_path(@task) %> </p>

  <p> <%= link_to 'Delete this task', task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Delete #{@task.name}?" }  %></p>
</div>
